name: Build and Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install mise
        uses: jdx/mise-action@v2

      - name: Setup environment
        run: mise run setup

      - name: Check formatting
        run: mise run format-check

      - name: Run unit tests
        run: mise run test

      - name: Build firmware (ESP-01S)
        run: mise run build:esp01

      - name: Build firmware (ESP32)
        run: mise run build:esp32

      - name: Upload firmware artifact (ESP-01S)
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: .pio/build/esp01_1m/firmware.bin

      - name: Upload firmware artifact (ESP32)
        uses: actions/upload-artifact@v4
        with:
          name: firmware-esp32
          path: .pio/build/esp32dev/firmware.bin

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: build-${{ github.sha }}
          name: Build ${{ github.sha }}
          files: |
            .pio/build/esp01_1m/firmware.bin
            .pio/build/esp32dev/firmware.bin
          draft: false
          prerelease: false
